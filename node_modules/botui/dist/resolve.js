export function resolveManager() {
    let resolver = (...args) => { };
    return {
        set: (callback) => {
            resolver = callback;
        },
        resolve: (...args) => resolver(...args),
    };
}
